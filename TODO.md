# TODO

- [x] Fix `GraphExpandRequest.debug: bool = True` default in `lethe/models/node.py` — debug output is on by default in production; should be `False`. Update the assertion in `tests/test_node_models.py:50` to match.
